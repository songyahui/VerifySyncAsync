

TRUE /\ {A, B} |- TRUE /\ ({A}.{B})^*;

TRUE /\ {A}.{B} |- TRUE /\ ({A, B})^*;

TRUE /\ {A, B} |- TRUE /\ ({A})^*;

TRUE /\ {A}.{B} |- TRUE /\ ({A}. {B})^*;

TRUE /\ {A, B} |- TRUE /\ ({A}.{B} \/ {A})^*;

TRUE /\ {A}.{B} \/ ({A, B}. {B, C}) |- TRUE /\ ({A}. {B})^*;

TRUE /\ ({A})^* |- TRUE /\ ({A})^*;

TRUE /\ ({A, B})^* |- TRUE /\ ({A}.{B})^*;

TRUE /\ ({A}.{B})^* |- TRUE /\ ({A, B})^*;

TRUE /\ ({A}.{A})^* |- TRUE /\ ({A})^*;